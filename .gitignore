backend/.venv/
backend/__pycache__

# SQLite index — never in git, always derived from YAML
index.db
# Studio data — each project (.studio/projects/<name>/) is its own git repo
.studio/
.studio/*

backend/*/__pycache__

.env

**/.DS_Store